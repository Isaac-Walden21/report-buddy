rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access.
    // All data access goes through Cloud Functions using the Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
